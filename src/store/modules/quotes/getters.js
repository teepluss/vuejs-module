// export const allQuotes = state => state.quotes.all
// export const title = state => state.quotes.title

export default {
  all: state => {
    return state.all
  },
  title: state => {
    return state.title
  }
}
